int main()
{
	int x // that number who is power of 2? possiblity is to check
	bool k;
	x & --x == 0? k=yes : k=no; // A number is power of two if " & " operation between given number and it's previous number is zero
	
	// ++x/--x -> increase first then use    or     x++/x-- -> use first then increase; 
}